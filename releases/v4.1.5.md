# v4.1.5

Patch release for configuration page dirty-state handling.

## Highlights

- Uses the same registry-status inference for baseline and draft company rows.
- Prevents enriched auto-tracked rows from making Configuration open with
  `Cancel` / `Save changes` visible by default.
- Keeps existing production config data intact while rendering legacy rows as
  auto-tracked when source/sample URL signals are present.

## Validation

- `npm run lint`
- `npm run build`
- `git diff --check`
