# v4.1.10

Patch release focused on profile privacy tooling and post-reset dashboard correctness.

## Fixes

- Fixed Clear job data so mounted dashboard, applied, jobs, action-plan, and logs widgets immediately reset to empty data after a confirmed wipe instead of showing stale analytics.
- Fixed the Profile export flow for right-to-access preparation by generating a JSON export from the existing authenticated job, applied-job, action-plan, config, and dashboard APIs instead of calling a missing production endpoint.
- Moved Sign out above Export my data in the Danger zone so the account actions follow the requested order.

## Validation

- `npm run lint`
- `npm run build`
- `git diff --check`
