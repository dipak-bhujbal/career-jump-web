# v3.0.1

Patch release for the v3.0 line that restores a clean lint gate after the AWS
separation release.

## Highlights

- Aligns ESLint with the repo's TanStack Router export pattern and existing
  controlled UI state-sync patterns.
- Removes stale eslint-disable comments and fixes the concrete
  `no-useless-assignment` warning in jobs sorting.
- Stabilizes memo dependencies in applied jobs and action-plan routes.
- Fixes meteor canvas cleanup so the animation state is captured safely for
  effect teardown.

## Validation

- `npm run lint`
- `npm run build`
- `git diff --check`
