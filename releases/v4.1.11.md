# v4.1.11

Patch release focused on applied-job source-of-truth consistency.

## Fixes

- Fixed applied-job API normalization in the React query layer so Dashboard, Profile, Applied Jobs, and export flows read the same nested job shape.
- Prepared the backend clear-data fix so clearing job data stores an explicit empty tenant applied-job state instead of falling back to legacy/global applied records.
- Confirmed the prior dashboard/profile mismatch was caused by stale backend applied records plus a flattened-vs-nested applied-job contract mismatch.

## Validation

- `npm run lint`
- `npm run build`
- `git diff --check`
- Backend `npm run check`
